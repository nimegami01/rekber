import { ConvertToAgoPipe } from './convert-to-ago.pipe';

describe('ConverToAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
