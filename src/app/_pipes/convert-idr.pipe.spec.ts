import { ConvertIdrPipe } from './convert-idr.pipe';

describe('ConvertIdrPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertIdrPipe();
    expect(pipe).toBeTruthy();
  });
});
