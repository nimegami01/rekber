import { SecondToDatePipe } from './second-to-date.pipe';

describe('SecondToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
